services:
  traffic_analytics_db:
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres123
      POSTGRES_DB: TrafficAnalyticsDB
    ports:
      - '5433:5432'
    volumes:
      - trafficanalyticsdb_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB} -h 127.0.0.1"]
      interval: 10s
      timeout: 5s
      retries: 10
    restart: unless-stopped

volumes:
  trafficanalyticsdb_data:
    name: trafficanalyticsdb_data
    driver: local
