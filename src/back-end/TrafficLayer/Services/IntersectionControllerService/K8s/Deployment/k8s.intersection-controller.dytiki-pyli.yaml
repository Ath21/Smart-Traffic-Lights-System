apiVersion: apps/v1
kind: Deployment
metadata:
  name: intersection-controller-dytiki-pyli-depl
  namespace: uniwa-stls
  labels:
    app: intersection-controller-dytiki-pyli
spec:
  replicas: 1
  selector:
    matchLabels:
      app: intersection-controller-dytiki-pyli
  template:
    metadata:
      labels:
        app: intersection-controller-dytiki-pyli
    spec:
      containers:
        - name: intersection-controller-dytiki-pyli-container
          image: ath21/stls-services:intersection-controller
          imagePullPolicy: Always
          ports:
            - containerPort: 8080

          # --- Load from ConfigMap and Secret ---
          envFrom:
            - configMapRef:
                name: intersection-controller-config
            - secretRef:
                name: intersection-controller-secret
          
          env:
            # --- Intersection Details ---
            - name: "Intersection__Id"
              value: "3"
            - name: "Intersection__Name"
              value: "Dytiki Pyli"

            # --- Traffic Lights ---
            - name: "TrafficLights__0__Id"
              value: "301"
            - name: "TrafficLights__0__Name"
              value: "Dytiki Pyli"
            - name: "TrafficLights__1__Id"
              value: "302"
            - name: "TrafficLights__1__Name"
              value: "Dimitsanas North"
            - name: "TrafficLights__2__Id"
              value: "303"
            - name: "TrafficLights__2__Name"
              value: "Dimitsanas South"

          # --- Probes ---
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3

          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3

          # --- Resource Limits ---
          resources:
            requests:
              cpu: 150m
              memory: 256Mi
            limits:
              cpu: 400m
              memory: 512Mi

      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: intersection-controller-dytiki-pyli-svc
  namespace: uniwa-stls
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: intersection-controller-dytiki-pyli
