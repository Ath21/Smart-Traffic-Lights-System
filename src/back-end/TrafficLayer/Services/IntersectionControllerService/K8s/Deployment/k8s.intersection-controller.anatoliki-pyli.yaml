apiVersion: apps/v1
kind: Deployment
metadata:
  name: intersection-controller-anatoliki-pyli-depl
  namespace: uniwa-stls
  labels:
    app: intersection-controller-anatoliki-pyli
spec:
  replicas: 1
  selector:
    matchLabels:
      app: intersection-controller-anatoliki-pyli
  template:
    metadata:
      labels:
        app: intersection-controller-anatoliki-pyli
    spec:
      containers:
        - name: intersection-controller-anatoliki-pyli-container
          image: ath21/stls-services:intersection-controller
          imagePullPolicy: Always
          ports:
            - containerPort: 8080

          # --- Load from ConfigMap and Secret ---
          envFrom:
            - configMapRef:
                name: intersection-controller-config
            - secretRef:
                name: intersection-controller-secret
          
          env:
            # --- Intersection Details ---
            - name: "Intersection__Id"
              value: "2"
            - name: "Intersection__Name"
              value: "Anatoliki Pyli"

            # --- Traffic Lights ---
            - name: "TrafficLights__0__Id"
              value: "201"
            - name: "TrafficLights__0__Name"
              value: "Anatoliki Pyli"
            - name: "TrafficLights__1__Id"
              value: "202"
            - name: "TrafficLights__1__Name"
              value: "Agiou Spyridonos"

          # --- Probes ---
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3

          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3

          # --- Resource Limits ---
          resources:
            requests:
              cpu: 50m   # instead of 100-150m
              memory: 128Mi
            limits:
              cpu: 200m  # instead of 300-400m
              memory: 256Mi


      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: intersection-controller-anatoliki-pyli-svc
  namespace: uniwa-stls
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: intersection-controller-anatoliki-pyli
