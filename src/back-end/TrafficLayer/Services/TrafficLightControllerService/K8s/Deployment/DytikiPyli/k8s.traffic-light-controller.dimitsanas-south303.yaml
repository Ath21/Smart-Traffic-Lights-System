apiVersion: apps/v1
kind: Deployment
metadata:
  name: traffic-light-controller-dimitsanas-south303-depl
  namespace: uniwa-stls
  labels:
    app: traffic-light-controller-dimitsanas-south303
spec:
  replicas: 1
  selector:
    matchLabels:
      app: traffic-light-controller-dimitsanas-south303
  template:
    metadata:
      labels:
        app: traffic-light-controller-dimitsanas-south303
    spec:
      initContainers:
        - name: wait-for-rabbitmq
          image: busybox:1.36
          command:
            - sh
            - -c
            - >
              echo "Waiting for RabbitMQ..." &&
              until nc -z rabbitmq-svc 5672; do
                echo "RabbitMQ not ready, sleeping...";
                sleep 3;
              done;
              echo "RabbitMQ is up!"
        - name: wait-for-traffic-cache
          image: redis:7
          command:
            - sh
            - -c
            - >
              echo "Waiting for Traffic Light Cache (Redis)..." &&
              until redis-cli -h traffic-light-cache-db -a "$REDIS_PASSWORD" ping | grep -q PONG; do
                echo "Redis not ready, sleeping...";
                sleep 3;
              done;
              echo "Redis is up!"
      containers:
        - name: traffic-light-controller-dimitsanas-south303-container
          image: ath21/stls-services:traffic-light-controller
          imagePullPolicy: Always
          ports:
            - containerPort: 8080

          # --- Load from ConfigMap and Secret ---
          envFrom:
            - configMapRef:
                name: traffic-light-controller-config
            - secretRef:
                name: traffic-light-controller-secret

          env:
            # --- Intersection Details ---
            - name: "Intersection__Id"
              value: "3"
            - name: "Intersection__Name"
              value: "Dytiki Pyli"

            # --- Traffic Lights ---
            - name: "TrafficLights__Id"
              value: "303"
            - name: "TrafficLights__Name"
              value: "Dimitsanas South"

          # --- Probes ---
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3

          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3

          # --- Resource Limits ---
          resources:
            requests:
              cpu: 50m   # instead of 100-150m
              memory: 64Mi
            limits:
              cpu: 200m  # instead of 300-400m
              memory: 128Mi

      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: traffic-light-controller-dimitsanas-south303-svc
  namespace: uniwa-stls
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: traffic-light-controller-dimitsanas-south303
