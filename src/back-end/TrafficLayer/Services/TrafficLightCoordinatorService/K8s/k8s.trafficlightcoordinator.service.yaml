apiVersion: v1
kind: Service
metadata:
  name: traffic-light-coordinator-api
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30220
  selector:
    app: traffic-light-coordinator-api
