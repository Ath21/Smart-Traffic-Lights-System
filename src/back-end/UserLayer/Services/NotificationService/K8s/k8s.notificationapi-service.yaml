apiVersion: v1
kind: Service
metadata:
  name: notification-api
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30087   # exposed on minikube
  selector:
    app: notification-api
