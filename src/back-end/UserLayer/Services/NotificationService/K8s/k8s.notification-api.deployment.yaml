apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-api
  namespace: uniwa-stls
  labels:
    app: notification-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-api
  template:
    metadata:
      labels:
        app: notification-api
    spec:
      containers:
        - name: notification-api-container
          image: ath21/stls-services:notification-api
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          
          # Load configuration from ConfigMap + Secret
          envFrom:
            - configMapRef:
                name: notification-api-config
            - secretRef:
                name: notification-api-secret

          # --- Probes ---
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3

          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3

          # --- Resource Limits ---
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi

      restartPolicy: Always
