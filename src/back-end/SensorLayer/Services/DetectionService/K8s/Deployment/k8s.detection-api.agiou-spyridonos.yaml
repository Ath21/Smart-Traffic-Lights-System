apiVersion: apps/v1
kind: Deployment
metadata:
  name: detection-api-agiou-spyridonos
  namespace: uniwa-stls
  labels:
    app: detection-api-agiou-spyridonos
spec:
  replicas: 1
  selector:
    matchLabels:
      app: detection-api-agiou-spyridonos
  template:
    metadata:
      labels:
        app: detection-api-agiou-spyridonos
    spec:
      containers:
        - name: detection-api-agiou-spyridonos-container
          image: ath21/stls-services:detection-api
          imagePullPolicy: Always
          ports:
            - containerPort: 8080

          # --- Load from ConfigMap and Secret ---
          envFrom:
            - configMapRef:
                name: detection-api-config
            - secretRef:
                name: detection-api-secret

          # --- Dynamic Intersection Info ---
          env:
            - name: Intersection__Id
              value: "1"
            - name: Intersection__Name
              value: "Agiou Spyridonos"

          # --- Probes ---
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3

          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3

          # --- Resource Limits ---
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi

      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: detection-api-agiou-spyridonos
  namespace: uniwa-stls
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: detection-api-agiou-spyridonos
